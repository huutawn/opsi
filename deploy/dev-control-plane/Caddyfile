:80 {
	@internal path /internal/* /api/internal/* /metrics
	respond @internal 404

	header {
		X-Content-Type-Options "nosniff"
		Referrer-Policy "no-referrer"
		X-Frame-Options "DENY"
	}

	reverse_proxy cloud:9800
}
