# Local Manual UI Parity Matrix

R5-014 maps the 21 R5-013 `SUPPORTED` capabilities to one Local API and one factual UI path. Every mutation uses the shared review dialog, a stable idempotency key, exact target confirmation where required, and backend-derived success evidence. FE-01 through FE-04 complete the frontend source redesign and mock Local API browser acceptance. Live Agent/VPS acceptance and the full manual gate remain pending at R5-017.

| Capability | CLI command | Local API route | UI view/navigation | Kind | Loading / success evidence | Confirmation / failure / retry | Permission / offline | Automated test |
|---|---|---|---|---|---|---|---|---|
| PAT keychain login | `opsi login --pat-file` | `/api/local/session/login/start`, `/api/local/session/callback` | Login gate / Settings | mutation | Session becomes authenticated from Local response | Redirect review; typed auth error and retry | Local session only; Cloud outage is distinct | `start_test.go`, Playwright harness |
| PAT verify, rotate, revoke, session | `opsi auth verify|rotate|revoke` | `/api/local/session`, `/api/local/session/token/{rotate,revoke}` | Settings | read/mutation | `authenticated`, `revoked`, or `rotated` response | PAT mutations show project and risk; same key retries | 401 returns to login; no PAT in browser | `start_test.go`, `fe04.spec.ts`, settings source test |
| Project list, create, switch | `opsi project list|create` | `/api/local/projects`, `/api/local/session/project` | Projects / project picker | read/mutation | Factual project row or created project ID | Create review includes org, name, slug, idempotency | Project switch clears project-specific state; Cloud outage preserves config | `start_test.go`, Playwright harness |
| Runtime inventory | `opsi runtime list|get` | `/api/local/projects/:id/topology/facts` | Runtime | read | Runtime, node, and agent facts | Typed error with retry | Agent outage shows `AGENT_UNAVAILABLE`, Cloud facts remain | runtime source test |
| Node inventory and diagnostics | `opsi node list|get` | `/api/local/projects/:id/nodes[/:node]` | Servers / Nodes | read | Node status and diagnostics | Typed error and Retry | Permission error is visible; Agent outage disables Agent actions | existing node tests |
| Node offline, drain, remove | `opsi node offline|drain|remove` | `/api/local/projects/:id/nodes/:node/{offline,drain,remove}` | Servers / Nodes | mutation | Returned node/job status | Remove requires exact node ID; same review key on retry | Disabled action explains Agent/permission reason | node tests, source scans |
| Bootstrap create, status, events, retry | `opsi server bootstrap|status|events|retry` | `/api/local/projects/:id/bootstrap-sessions[/:session/events|/retry]` | Servers / Nodes bootstrap timeline | mutation/read | Session status and event timeline | Credential form clears after submission; retry reuses key | Agent outage preserves Cloud session history | existing bootstrap tests |
| GitHub installation inventory and claim | `opsi github installation ...` | `/api/local/projects/:id/github/installations`, `/claim/*` | GitHub | read/mutation | Installation inventory or Cloud claim receipt | Claim review identifies numeric installation | Redirect is Local callback; no browser credential | existing GitHub tests |
| Repository inventory, claim, binding | `opsi github repository|binding ...` | `/api/local/projects/:id/github/{repositories,bindings}` | GitHub | read/mutation | Repository ownership and binding IDs | Exact repository/binding confirmation | Cloud permission and outage states are factual | existing GitHub tests |
| Repository initialization and CD config | `opsi init`, `opsi cd ...` | `/api/local/repository/{config,config/preview,apply,plan/preview}` | GitHub / repository CD | read/mutation | Preview hash and applied config hash | Preview hash and idempotency key required | Local repository errors are retryable; no legacy source-build form | repository-CD tests |
| Services/catalog | `opsi service ...` | `/api/local/projects/:id/services` | Services | read/mutation | Service record returned by Cloud/Agent | Create review names service target | Agent outage disables Agent-dependent actions | existing service tests |
| Topology, dependencies, policy | `opsi topology ...`, `opsi policy ...` | `/api/local/projects/:id/{topology,deployment-policies}/*` | Topology | read/mutation | Plan/policy hashes and revision | APPLY/disable exact confirmation; stale state requires new preview | Permission and unknown capacity are explicit | topology tests |
| Secret create, rotate, reveal | `opsi secret create|rotate|reveal` | `/api/local/projects/:id/secrets[/:name/{reveal,rotate}]` | Security / Secrets | mutation | Sanitized Agent receipt or protected reveal | Review includes explicit service/name/namespace and contextual second factor | `Cache-Control: no-store`; Agent outage is not empty success | secret tests, `fe04.spec.ts`, source scans |
| TOTP setup | `opsi secret setup-totp` | `/api/local/projects/:id/secrets/setup-totp` | Security / Secrets | mutation | Agent URI/secret with backend TTL | Review and same idempotency key | Sensitive result cleared on hide, Escape, TTL, navigation, project switch, auth invalidation, and logout | `start_test.go`, `fe04.spec.ts`, source tests |
| BuildRecord list and detail | `opsi build-record list|get` | `/api/local/projects/:id/build-records[/:record]` | Build Records | read | Immutable digest/workload evidence | Read-only, no deploy shortcut | Cloud error has Retry | existing BuildRecord tests |
| Deployment preview, apply, status, events, rollback | `opsi deploy ...` | `/api/local/projects/:id/deployments*` | Deployments | read/mutation | Preview snapshot, job status, events | Apply/rollback reviewed; rollback requires exact job ID | Agent outage keeps loaded Cloud history and disables actions | deployment tests |
| Exposure preview, apply, history | `opsi exposure ...` | `/api/local/projects/:id/exposures*` | Deployments / Exposure rollout | read/mutation | Desired/current/previous digest and rollout state | Exact target review; backend response is success | Cloud/Agent outage states are distinct | exposure tests |
| Telemetry and redacted logs | `opsi telemetry query`, `opsi sync` | `/api/local/projects/:id/{telemetry,logs}` | Metrics / Logs | read | Redacted summary or text rows; missing counters are `Unknown` | Invalid query gives typed Retry | Agent unavailable is shown, not fabricated as zero; unresolved identity stays separate | metrics tests, `fe04.spec.ts` |
| Incidents | `opsi incident list|get|resolve` | `/api/local/projects/:id/incidents*` | Incidents | read/mutation | Factual incident status and request ID | UI hands off to factual `opsi action` CLI preflight/approve/execute commands | Malformed evidence fails closed; private approval material stays in the OS secure store | incident tests, `fe04.spec.ts` |
| Audit | `opsi audit list` | `/api/local/projects/:id/audit` | Security / Audit | read | Actor, action, resource, result, and bounded selected detail | Loaded-history filters are read-only | Human/machine actors remain distinct; unavailable is not empty | audit tests, `fe04.spec.ts` |
| Version, configuration, install | `opsi version`, installer | `/api/local/settings`; installed `opsi-ui` | Settings | read | Version/revision, authorities, asset directory | No mutation except PAT controls | Missing assets are a factual startup error | settings test, installer self-test |
| Organization listing | none | none | Settings backend gaps | unavailable | `BACKEND_GAP` with R5-017+ roadmap | No enabled control | Not silently replaced by org fallback | settings source test |
| Members/RBAC | none | none | Settings backend gaps | unavailable | `BACKEND_GAP` with R5-017+ roadmap | No enabled control | Permission model remains backend-owned | settings source test |
| Secret metadata/listing | none | none | Secrets backend gaps | unavailable | `BACKEND_GAP` with R5-017+ roadmap | No enabled control | Secret values are never guessed or listed | secrets source test |

Totals: `21/21 SUPPORTED` mapped; `3/3 BACKEND_GAP` shown truthfully; `0` unexplained placeholders; `0` fake operations. FE-01 through FE-04 source redesign and browser fixture acceptance pass; no live Agent/VPS acceptance or production-readiness claim is made.
