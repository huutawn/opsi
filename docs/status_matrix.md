# Opsi Status Matrix

Canonical target requirements: `docs/opsi_srs.md`. Current-state detail:
`docs/current_state.md`. Roadmap execution order:
`docs/opsi_roadmap_v5_production.md`.
Trusted artifact target:
`docs/architecture_decisions/ADR-004-trusted-artifact-cd.md`.

Allowed statuses: `DONE`, `PARTIAL`, `OPERATOR_REQUIRED`, `CONTRACT_ONLY`, `DOC_ONLY`,
`NOT_STARTED`, `FAILED_OR_REGRESSED`, `BLOCKED`, `UNPROVEN`, `MANUAL_GATED`.

`DONE` means the exact row is supported by code, tests, executable checks, and
truthful documentation. It does not imply production readiness. `DOC_ONLY`
means the documented target and boundaries are established but runtime
capability is not implemented.

Legacy `Pxx` labels retained in evidence text identify historical implementation
prompts only. They do not define current execution order; all new work follows
`docs/opsi_roadmap_v5_production.md` and its `R5-xxx` gates.

| ID | Requirement / capability | Owner / boundary | Current status | Executable evidence | Missing work | Target gate | Priority |
|---|---|---|---|---|---|---|---|
| INCIDENT-FACTS-01 | Deterministic factual incident list/get/resolve, authorization, resolve audit, no legacy RCA execution | Agent -> CLI local backend | DONE | `contracts/agent/v1/status.proto`; `agent/internal/incident/service.go`; `TestListGetResolveAuthorizationIsPreserved`; `TestResolveIncidentDoesNotDependOnLegacyRCA`; `TestIncidentServiceDescriptorContainsOnlyActiveIncidentRPCs`; `go test ./...` from `agent/` and `cli/` | Public evidence bundle and real-infra incident artifact are P22 | P22 evidence gate | P0 |
| AI-BOUNDARY-RESET-01 | Cloud AI runtime and Agent analyzer/fallback/RCA-backed execution are removed | Cloud + Agent + CLI/contracts | DONE | `TestAIRoutesRemoved`; `TestRemovedLocalIncidentRoutesReturnNotFound`; `TestIncidentHelpContainsOnlyActiveCommands`; deletion grep; `make test` | User-owned instruction/MCP integration begins only at R5-018 after the R5-017 manual gate | R5-018–R5-020 | P0 |
| INCIDENT-EVIDENCE-01 | Public versioned `IncidentEvidence v1` with diff/timeline/events/redacted excerpts/hash/injection tags | Agent evidence boundary | NOT_STARTED | No `IncidentEvidence` symbol in runtime/contracts; `rg -n 'IncidentEvidence' agent cli cloud contracts` | Implement contract, builder, APIs, redaction, and real-failure proof | R5-015 | P0 |
| SAFE-ACTION-PLANE-01 | `ActionPlan`, deterministic preflight/risk, separate approval grant, typed executors, locks/post-check/audit | Agent policy/execution + trusted CLI approval | NOT_STARTED | No `ActionPlan` contract in runtime/contracts | Implement the manual CLI/UI action boundary without coupling trusted CD to AI approval | R5-016 | P0 |
| CLI-MCP-01 | User-owned vendor-neutral `opsi mcp serve`; read/preflight/challenge tools only | CLI local backend | NOT_STARTED | No MCP runtime files/symbols | Implement only after R5-017; expose no execute/approve tool | R5-019–R5-020 | P0 |
| GATEWAY-01 | Opsi-rendered Deployment, ClusterIP Service, Traefik exposure, conflicts/readiness/rollback | Agent deployment/gateway | NOT_STARTED | `TestLoadRejectsRemovedIngressEnabledConfig`; `TestDeployHelpDoesNotAdvertiseIngress`; active runtime has no managed gateway | Implement digest deployment, exposure, readiness, reconciliation, and rollback in roadmap order | R5-010–R5-011 | P0 |
| E2E-K3S-01 | Clean VPS/K3s factual product-path proof including incident list/get/resolve and resolve audit | Protected manual workflow | MANUAL_GATED | `scripts/e2e/verify-k3s.sh`; `.github/workflows/e2e-k3s.yml`; `docs/runbooks/clean_vps_k3s_e2e.md`; R5-004 live bootstrap/registration/reboot subset at `d3df6b8d` | The wider deployment, secret, telemetry, incident, and audit scenario still has no accepted full real-infrastructure artifact | R5-017 | P0 |
| LIVE-BOOTSTRAP-R5-004 | Separate clean Ubuntu 24.04 VPS bootstraps pinned K3s and Agent through CLI -> Cloud session -> Worker strict SSH, then CLI/Local UI share factual state and services survive reboot | CLI + Cloud + Worker + Agent VPS | PARTIAL / FUNCTIONAL_ACCEPTANCE_PASS / RESILIENCE_EVIDENCE_DEFERRED | Historical `boot-97705a044b859f66`; accepted R5-004D Gate B; recovery `boot-7b843526dff6842b` completed as current Agent node `node-c69fe70180d359d7` | The single authorized Worker restart poller missed `install_k3s`; no second recovery/reboot is authorized. R5-017 must prove the restart on a disposable VPS or fresh reset with a deterministic staging-only barrier/fault mechanism; R5-018/MCP is blocked until then. | R5-017 deferred resilience gate | P0 |
| SOURCE-HYGIENE-01 | Source/package candidates exclude local config, credentials, private keys, runtime state, databases, logs, generated output, unsafe paths, and escaping symlinks | Repository/release tooling | DONE | `scripts/source-package.sh`; `scripts/source-package-test.sh`; `make verify-source-package-policy`; `make source-hygiene`; `make package-source`; `make check-source-package` | Keep the Git-aware candidate set and pre-publication validation as the only packaging path | R5-001 containment gate | P0 |
| CREDENTIAL-INCIDENT-R5-001 | Credentials present in a historical working-tree source archive are treated as potentially disclosed | Repository owner + credential operators | OPERATOR_REQUIRED | Root-cause history identifies the former `tar ... .` recipe; packaging policy tests and canonical artifact validation provide containment evidence; `docs/runbooks/credential-incident.md` defines response | Rotate or revoke every applicable credential class, verify replacements and old-credential rejection, scan all Git refs and distributed artifacts, and record the owner decision on history cleanup | R5-001 incident closure | P0 |
| STAGING-CONTROL-PLANE-R5-002 | Separate production-like staging profile with origin TLS configuration, fail-closed production flags, synchronized PostgreSQL DSN credentials, explicit staging-only internal HTTP opt-in, hardened containers, private backend services, and public internal-route denial | Staging control-plane source/config | DONE | `deploy/staging-control-plane/*`; ordered Caddy loopback health route; `scripts/validate-staging-control-plane.py`; 21 focused validator tests; `scripts/e2e/verify-staging-control-plane-caddy.sh`; dev and staging source/Compose validation | The R5-003 isolated Origin CA smoke passes; public-port cutover and external evidence remain separate operator gates | R5-002 source/config gate | P0 |
| ORIGIN-TLS-LIVE-R5-003 | Public origin serves a hostname-valid, unexpired, trusted certificate on HTTPS and survives controlled restart/rotation | Operator + staging origin | PARTIAL | 2026-07-17 direct-origin certificate/key/Origin CA checks, public 80/443 cutover, HTTPS hostname validation, protected-route matrix, and proxy restart/down-up pass at revision `d5b2e81c433d287369ad63e99fc4331db68bc420` | Certificate rotation and direct-origin firewall restriction remain operator work; no business rows existed for identifier recovery | R5-003 | P0 |
| CLOUDFLARE-FULL-STRICT-R5-003 | Cloudflare connects to the staging origin with certificate validation in Full (strict) mode | Operator + Cloudflare | DONE | Operator enabled Full (strict); public `/health` returned 200 with Cloudflare edge headers, no 525/526, HTTP redirect passed, public TLS chain passed, protected routes returned 404, and error responses were `cf-cache-status: DYNAMIC` | Keep monitoring callback/webhook behavior; direct-origin restriction is separate operator work | R5-003 | P0 |
| STAGING-RESTART-PERSISTENCE-R5-003 | Staging PostgreSQL, Cloud, Worker, and proxy recover with durable state and correct exposure after host/service restart | Operator + staging VPS | DONE | Independent proxy/Worker/Cloud/PostgreSQL restarts, Worker recovery windows, Compose down/up without `-v`, 4/4 health, public health, stable 33-table schema hash, and unchanged named-volume metadata passed | Business-row persistence was not exercised because the live database had zero users/projects/nodes/sessions | R5-003 | P0 |
| M0-DOC-TRUTH-01 | Active SRS, architecture, status, security, ADR, and E2E docs separate current from target and remove old AI boundary claims | Documentation | DONE | `docs/opsi_srs.md`; `docs/architecture.md`; `docs/security_story.md`; ADR-003; documentation grep gates; `git diff --check` | Continue enforcing the boundary as later tasks change implementation | Baseline | P0 |
| ROADMAP-V5-DOC-01 | One canonical active R5-001–R5-024 production roadmap orders manual CLI/UI acceptance before MCP without claiming implementation | Documentation | DOC_ONLY | `docs/opsi_roadmap_v5_production.md`; canonical-document roadmap grep; `git diff --check` | Execute R5-003–R5-024 in order; R5-001 remains `OPERATOR_REQUIRED` until the incident operator work is recorded | Roadmap v5 synchronization | P0 |
| AGENT-EXECUTABLE-01 | One `opsi-agent` executable composes existing config validation and Agent runtime with required config, check, version, help, signals, and stable exit codes | Agent executable | DONE | `GOCACHE=/tmp/opsi-go-cache GOTOOLCHAIN=local go test ./cmd/opsi-agent -count=1`; `GOCACHE=/tmp/opsi-go-cache GOTOOLCHAIN=local go test ./internal/config ./internal/server -count=1`; `GOCACHE=/tmp/opsi-go-cache GOTOOLCHAIN=local go test ./... -count=1` from `agent/`; `make build`; `./bin/opsi-agent --version`; temporary config `--check`; `make release`; `make smoke-release` | Runtime implementation remains owned by `agent/internal/server`; no VPS runtime proof in P03 | P03 | P0 |
| AGENT-RELEASE-ARTIFACT-01 | Deterministic direct Linux amd64 Agent binary with explicit version/full commit, SHA-256 checksum, stable JSON manifest, and byte-for-byte local reproducibility check | Agent release tooling | DONE | `make agent-release AGENT_VERSION=0.0.0-test AGENT_COMMIT=0000000000000000000000000000000000000000`; `sha256sum --check dist/agent/checksums.txt`; Python metadata/hash assertions; `make verify-agent-release`; `make release`; `make smoke-release` | Artifact is not hosted over HTTPS; Bootstrap Worker integration, canonical systemd install layout, upgrade/rollback, and target VPS evidence remain P05-P06 | P03 | P0 |
| GITHUB-APP-01 | Separate GitHub App user authorization, installation authentication, verified webhook inventory, repository/service ownership, and CLI/UI bootstrap | Cloud identity/GitHub control plane | OPERATOR_REQUIRED | Live projectless callback `auth=ok`; Bearer keychain verification; idempotent installation/repository claims; two active bindings; init dry-run/apply/idempotency; CLI/Local API parity; signed `added` delivery and PostgreSQL duplicate after Cloud recreation; full CLI/Cloud test+vet, PostgreSQL focused tests, UI lint/build, sanitized preflight | GitHub App API has no fixture `removed` or `repository` delivery; a second GitHub account is unavailable for the canonical live wrong-user negative. Obtain those artifacts without raw payloads before `DONE` | R5-005 / P07-P10 | P0 |
| GITHUB-APP-USER-AUTH-01 | GitHub App user authorization replaces generic OAuth while preserving browser start/callback/redeem, mandatory prelinking, projectless single-membership resolution, and no automatic user creation | Cloud identity boundary | DONE | PKCE/provider/prelink tests; projectless membership tests; typed callback redirects; `TestPATVerifyUsesBearerAuthorizationAndDoesNotAcceptBodyToken`; live projectless callback `auth=ok`; post-redeploy Local session reports authenticated/valid/Cloud connected | Multi-project login remains fail-closed until a project is selected; live wrong-user still requires a second GitHub account under the parent R5-005 gate | P07 / R5-005 | P0 |
| GITHUB-APP-PKCE-01 | Every GitHub user authorization uses PKCE S256, crypto-random one-time state, bounded token/user responses, and crypto-random one-time local grants | Cloud browser-auth boundary | DONE | One-time/expiry/random-failure tests plus live projectless GitHub redirect/callback/redeem ending `auth=ok` | State/grants are not HA-safe in P07; reused/expired negatives are proven by focused tests without exposing browser credentials | P07 / R5-005 | P0 |
| GITHUB-APP-JWT-01 | Cloud loads a protected RSA private key and signs minimal RS256 App JWTs with bounded GitHub-compatible claims | Cloud GitHub App authentication | DONE | PKCS#1/PKCS#8, rejection, claims, timing, public-key signature tests, and sanitized live `/app` plus installation-token preflight | Private-key replacement requires Cloud restart; key and JWT values never enter evidence | P08 / R5-005 | P0 |
| GITHUB-INSTALLATION-TOKEN-01 | Fixed-endpoint installation access-token client validates bounded responses and coalesces concurrent refreshes per installation | Cloud GitHub App authentication | DONE | Header/endpoint/body, response validation, redirect, cache/refresh/failure, and concurrency tests in `github_app_client_test.go`; sanitized live installation-token request passes for installation `147333403` | Tokens remain memory-only and are never included in evidence | P08-P09 / R5-005 | P0 |
| GITHUB-APP-WEBHOOK-01 | Separate App-wide webhook endpoint verifies SHA-256 signatures before decoding and emits typed installation/repository events | Cloud GitHub App webhook boundary | DONE | Signature-order/limit, typed event, unknown action, malformed payload, nil-sink, error mapping, registry-sink tests; signed live `installation_repositories: added` returns 200 while unsigned input returns 401 | Live `removed` and `repository` deliveries remain absent and block parent R5-005 acceptance | P08-P09 / R5-005 | P0 |
| GITHUB-WEBHOOK-REPLAY-01 | Bounded 24-hour in-memory delivery ledger tracks in-flight/completed events and releases failed reservations for retry | Cloud GitHub App webhook boundary | DONE | Completed/in-flight duplicate, failure retry, expiry, capacity, and metadata-only state tests in `github_app_webhook_test.go` | This remains the fast per-process layer; PostgreSQL durable replay is tracked separately; live verification is `UNPROVEN` | P08 | P0 |
| GITHUB-INSTALLATION-INVENTORY-01 | Numeric installation/account identity and lifecycle metadata persist without physical deletion | Cloud registry | DONE | Inventory/PostgreSQL tests plus live active installation `147333403` for account `143307746`, matching through CLI and Local API after Cloud recreation | Missing live `removed` delivery is tracked by the parent R5-005 gate | P09 / R5-005 | P0 |
| GITHUB-REPOSITORY-INVENTORY-01 | Numeric repository/owner identity persists across rename, transfer, removal, deletion, and Cloud restart | Cloud registry | DONE | In-memory/PostgreSQL lifecycle tests plus live active repository `1304594095`; sparse `added` preserves authoritative metadata and restores active state | Live removal transition is not yet evidenced because GitHub exposes no `removed` delivery | P09 / R5-005 | P0 |
| GITHUB-WEBHOOK-DURABLE-REPLAY-01 | Delivery insertion and installation/repository mutation commit atomically; unique delivery ID deduplicates after Cloud restart | Cloud registry + webhook boundary | DONE | PostgreSQL restart/dedupe tests; live GUID `ff5d1740-82ad-11f1-8b7d-10bc8b43c4a0` returns `duplicate=true` after Cloud recreation and again through the focused verifier | P08 in-memory replay remains as the fast per-process layer; live evidence is sanitized metadata only | P09 / R5-005 | P0 |
| GITHUB-INSTALLATION-CLAIM-01 | Owner/Admin claim uses purpose-bound OAuth state, prelinked numeric GitHub identity comparison, `/user/installations` proof, bounded pagination, visible-repository sync, and a one-time claim grant | Cloud identity + registry | DONE | Claim/grant tests plus live installation claim repeated with one active installation and synchronized repository inventory | User token is never stored; organization proof establishes visibility, not org-owner status | P09 / R5-005 | P0 |
| GITHUB-REPOSITORY-CLAIM-01 | One active project owns a repository; same-project claim is idempotent, release requires no active binding, and project inventory exposes available/active/conflict state without leaking another project ID | Cloud registry + PAT/RBAC API | DONE | Concurrent/API tests plus live repeated claim of repository `1304594095`; release with active bindings returns typed 409; CLI/Local API inventory matches | Inventory remains project-scoped; cross-project non-disclosure is covered by focused tests and no other-project ID appears in live inventory | P09-P10 / R5-005 | P0 |
| GITHUB-SERVICE-BINDING-01 | Claimed repositories bind one or more validated service keys within the same project; each service has one active GitHub binding | Cloud registry + PAT/RBAC API | DONE | Binding/PostgreSQL/API tests plus live active `api` and `worker` bindings on repository `1304594095`, matching through CLI and Local API | Binding does not modify deployment source contracts and never targets Agent/Node/runtime/VPS identity | P09 / R5-005 | P0 |
| CLI-REPOSITORY-INIT-01 | `opsi init` validates service and target files, matches safe GitHub origin metadata, claims numeric repository identity, creates the P09 binding, and reruns idempotently | CLI -> Cloud + local repository | DONE | Focused/race CLI tests plus live disposable fixture clone: dry-run, apply creating config/workflow, second apply unchanged, remote HEAD unchanged | The disposable clone needed an untracked minimal Dockerfile because the fixture contains only README; nothing was committed or pushed | P10 / R5-005 | P0 |
| CLI-INSTALLATION-CLAIM-01 | `opsi github installation claim` and optional `opsi init` installation claim bind only loopback, use crypto-random local state, validate a one-time callback, redeem without persisting/logging grant, then refresh inventory | CLI + Cloud OAuth mediation | DONE | Callback mismatch/missing/one-time/timeout/no-browser tests plus live installation `147333403` claim and idempotent inventory refresh | Cloud performs GitHub OAuth verification; grants and tokens are not persisted or printed | P10 / R5-005 | P0 |
| LOCAL-GITHUB-APP-UI-01 | Browser uses only `/api/local/...` for one centered GitHub auth gate, installation/repository inventory, claim/release, and multi-service binding create/remove with retry and exact destructive confirmation | Local UI -> CLI local backend -> Cloud GitHub API | DONE | Projectless live sign-in; Bearer PAT session verification; matching live installation/repository/two-binding inventory through Local API and CLI; browser-storage/direct-Cloud source gate; typed conflict tests; UI lint/build | Parent R5-005 still waits for missing live webhook actions and wrong-user evidence, not UI parity | P10 / R5-005 | P0 |
| REPOSITORY-CD-CONFIG-01 | Strict deterministic v2 `.opsi/opsi-cd.yaml` contains multi-service build/deployment intent only; v1 migration and atomic add/update preserve unrelated services and reject unsafe/unknown data | CLI repository bootstrap + Local API/UI | DONE | v1/v2 migration, multi-service create/add/update/idempotency, duplicate/cycle/missing dependency, traversal/symlink, deterministic YAML, atomic mutation tests, plus focused `preview_hash`, stale-write rejection, bounded replay, and conflicting-key negatives | No project/VPS/Cloud identity, BuildRecord, OCI push, or deployment exists in this task | R5-006 local acceptance + R5-007 focused entry repair | P0 |
| REPOSITORY-CD-PLAN-01 | Bounded changed-service resolver uses fixed NUL Git diff argv, path-aware matching, rename source/destination, shared paths, dependency closure, and fail-safe full build | CLI application service + Local API/UI | DONE | Resolver focused tests and disposable Git repo: one service, dependency closure, shared path, rename, empty diff, missing base, shallow/malformed/bounded diff, deterministic plan hash, CLI/API parity | Live GitHub event delivery is not used; runner proof belongs to R5-008 | R5-006 local acceptance | P0 |
| REPOSITORY-WORKFLOW-BOOTSTRAP-01 | Deterministic secure changed-service workflow matrix with read-only contents permission, immutable action SHA, timeout/concurrency, fork-safe no-push/no-deploy behavior | CLI repository bootstrap + Local API/UI | DONE / LIVE_ACCEPTANCE_PASS | YAML parse, full-SHA pin, minimal permissions, no `pull_request_target`, exact trusted push split, live baseline full matrix, live changed-service api-only matrix, and live PR publish skip | No Agent deploy action is present; final fixture workflow retains secure main behavior | R5-006 local acceptance / R5-008 live | P0 |
| GITHUB-OIDC-01 | GitHub Actions OIDC verifier with JWKS, claim policy, workflow allowlist, replay protection, and claim/body binding | Cloud trusted build boundary | DONE / LIVE_ACCEPTANCE_PASS | Local signed fixtures plus live GitHub-hosted runner wrong audience/workflow/ref, claim/body mismatch, exact issuer/audience, exact workflow/job ref, and public endpoint correlation | Direct workflows may emit `job_workflow_ref`; production policy now allowlists the exact observed value when present | P11 local / R5-008 live | P0 |
| BUILD-RECORD-01 | Versioned OIDC-bound `BuildRecord` with repository/run/attempt idempotency, image digest, and provenance metadata | Cloud build metadata boundary | DONE / LIVE_ACCEPTANCE_PASS | `opsi.build_record/v1`, strict OIDC submission, claim/stored-binding authorization, conflict-safe replay, append-only PostgreSQL migration, restart/concurrency tests, live GHCR digest equality, project-scoped CLI/Local API/UI parity, no-token storage checks | No Agent deployment is implemented in R5-008 | P12 local / R5-008 live | P0 |
| ARTIFACT-DEPLOY-01 | Production deployment uses allowlisted OCI repository and immutable `image@sha256:<digest>`; Agent pulls and deploys digest | Cloud routing -> Agent runtime -> OCI registry | NOT_STARTED | Current Agent rejects image-source jobs and still supports Git clone/build | Implement P14-P16; keep Git path explicitly non-production during migration | P14-P16 | P0 |
| TRUSTED-CD-POLICY-01 | Preconfigured `DeploymentPolicy` authorizes allowed repository/workflow/event/ref/environment delivery separately from AI/manual ActionPlane approval | Cloud policy/routing | NOT_STARTED | No trusted-CD policy implementation; documentation is target only | Implement policy, RBAC, idempotency, durable jobs, and audit in P15; AI must not approve CD | P15-P20 | P0 |
| PR-PREVIEW-01 | Same-repository PR preview policy with fork fail-closed, no production secrets, isolation, and TTL cleanup | Cloud policy + Agent runtime | NOT_STARTED | No preview deployment contract, environment lifecycle, or real GitHub proof | Implement and verify P21; PR approval is not production approval | P21 | P0 |
| BOOTSTRAP-DAEMON-01 | Bootstrap Worker polls, atomically leases one session, heartbeats, recovers expired leases, retries with persisted backoff, and dead-letters exhausted/permanent failures | Cloud bootstrap worker | PARTIAL | Durable checkpoint/resume tests; focused/race suites; R5-004 live lease, fail-closed dead-letter, supported manual retry, completion, post-completion Worker restart, and continued heartbeat | Live restart while a destructive step is in flight remains unproven without a safe fault hook | P04-P06 / R5-004 | P0 |
| BOOTSTRAP-CHECKPOINT-01 | Durable lease-bound per-step checkpoint with schema/version/fingerprint validation, monotonic idempotent transitions, and in-memory/PostgreSQL parity | Cloud registry + internal worker API | DONE | P04 registry/API tests; PostgreSQL 17 integration and pre-P04 row migration-upgrade tests pass; migration `p04_bootstrap_checkpoint_v1`; CLI status and Local UI checkpoint rendering | OTP/PAT baseline failure fixed; full Cloud suite PASS at this commit | P04 closure gate | P0 |
| BOOTSTRAP-RESUME-01 | Worker resumes `first-server-v2` from `next_step_index`, checkpoints after each successful remote step, skips SSH after all remote steps, then verifies Agent heartbeat; unfinished v1 checkpoints fail closed | Bootstrap Worker | PARTIAL | Resume/fingerprint/checkpoint/lease tests; R5-004 live checkpoint sequence, stable session/node idempotency, supported retry-before-step-zero, verified Agent heartbeat, and target reboot recovery | Live mid-step Worker restart and the registration pre-marker crash window remain unproven | P04-P06 / R5-004 | P0 |
| ADMIN-BOOTSTRAP-OWNER-01 | Explicit local idempotent first user/organization/project Owner provisioning plus marker-based OAuth recovery with no manual SQL | Cloud identity/control-plane metadata | DONE | `opsi-cloud admin bootstrap-owner`; `--link-existing-owner`; `adminbootstrap.Service.ProvisionBootstrapOwner`; `TestPostgresBootstrapOwnerIsIdempotentAcrossRestart`; `TestPostgresBootstrapOwnerLinksExistingMarkerOAuthIdentity`; `go test ./internal/adminbootstrap`; `make verify-postgres` gate | GitHub provider prelinking is used by P07/P09 identity comparison; live marker-based link still requires staging execution | Baseline | P0 |
| DEV-CONTROL-PLANE-PACKAGE | One supported development Docker Compose path for PostgreSQL, Opsi Cloud, one Bootstrap Worker, and Caddy | Development control plane | DONE | `deploy/dev-control-plane/compose.yaml`; `cloud/Dockerfile`; validation/build checks; non-root image inspection; configuration checks; local four-service `/health` smoke | Code status remains DONE. Clean control-plane VPS checkpoint `CP-VPS-1` is `DEFERRED / UNPROVEN` because no clean Ubuntu VPS was available; it remains required before production acceptance | P01 / CP-VPS-1 | P0 |
| PRODUCTION-READINESS-01 | Production security, durability, release, upgrade, rollback, DR, and repeated acceptance proof | Whole system | UNPROVEN | `docs/opsi_roadmap_v5_production.md`; no R5-024 acceptance artifact | Complete R5-003–R5-024 and every mandatory checkpoint; do not infer readiness from code-complete rows, documentation, unit tests, or command scaffolds | R5-024 | P0 |
