# Opsi Status Matrix

Canonical requirement source: `docs/opsi_srs.md` (SRS v4 active production-ready contract). Legacy SRS v3.x material is archived under `docs/archive/` and is historical only.

| Requirement ID | Requirement | Status | Evidence paths | Tests | Gaps | Next action |
|---|---|---|---|---|---|---|
| A-01 | Local-first Agent owns runtime execution/data | Partial | `agent/`; `cli/internal/commands/start.go`; `cli/ui/lib/api/local-client.ts`; `cli/ui/features/logs/logs-view.tsx`; `cli/ui/features/incidents/incidents-view.tsx`; `/api/local/session`; `/api/local/status`; `/api/local/projects...`; `/api/local/projects/{project_id}/secrets...`; `/api/local/projects/{project_id}/telemetry...`; `/api/local/projects/{project_id}/logs`; `/api/local/projects/{project_id}/incidents`; `/incidents/{incident_id}`; `/incidents/{incident_id}/analyze`; `/actions/{action_id}/approve`; `/resolve` | `make verify`; local session/mutation guard tests; local proxy keychain/PAT tests; `TestLocalSecretCreateUsesAgentNotCloudAndRedactsValue`; `TestLocalSecretRevealUsesAgentWithNoStorePolicy`; `TestLocalTelemetrySummaryUsesAgentAndHidesRawPayload`; `TestLocalLogsUseAgentNotCloudAndRedact`; `TestLocalIncidentListUsesAgentNotCloud`; `TestLocalIncidentAnalyzeUsesAgentNotCloud`; `TestLocalIncidentApproveRequiresExplicitApproval`; `TestLocalIncidentRejectsArbitraryCommand`; `TestLocalIncidentApproveSendsAllowlistActionHashToAgent`; UI build | Secret create/reveal/rotate, telemetry/logs, and incident list/detail/analyze/approve/resolve now use Agent; secret list/delete/audit and runtime audit merge still need Agent-backed local endpoints | Implement remaining Agent-owned local API facade areas |
| D-01 | Continuous deployment from Cloud relay to Agent | Partial | `agent/internal/cloudrunner`; `agent/internal/deploy`; `cloud/internal/registry`; `cloud/internal/webhookrelay`; Cloud lease/result APIs; `deployment_jobs` lease columns; `relay_jobs`; `relay_events` | CloudRunner, deploy manifest, rollback verification/redaction, registry intent, lease retry/dead-letter, local proxy, registry API tests, webhook relay sanitizer/idempotency tests; mandatory CI Postgres target `make verify-postgres` covers `TestPostgresDeploymentJobRestartRetryDeadLetterAndIdempotency`, `TestPostgresQueuePersistsSanitizedJobsWhenDatabaseAvailable`, `TestPostgresRelayRetryScheduleSurvivesRestart` | Progress events are terminal/coarse; deployment job and webhook relay queue state are durable when Postgres is configured; external notification/outbox durability remains future work | Expand progress streaming and durable notification/outbox without storing raw runtime payloads |
| SEC-01 | Project-scoped operational actions | Implemented | Agent/Cloud requests carry `project_id`; RBAC guards | RBAC and service/deploy tests | Matrix must expand as RPC surface grows | Keep project-scope tests mandatory for new operations |
| SEC-02 | PAT verify and role-aware access | Partial | `cloud/internal/auth`; `cloud/internal/webhookrelay/server.go`; `cli/internal/commands/start.go`; `cli/internal/keychain`; `cli/ui/lib/api/local-client.ts`; `cli/ui/components/layout/topbar.tsx`; `/api/local/session/login/start`; `/api/local/session/callback`; `/api/local/session/logout`; `/api/local/session/token/rotate`; `/v1/auth/browser/start`; `/v1/auth/browser/callback`; `/v1/auth/browser/redeem`; `/v1/auth/pat/rotate`; `/v1/auth/pat/revoke` | `TestPATIssueRotateAndRevoke`; `TestBrowserAuthFlowUsesOneTimeGrantAndAuditsWithoutPAT`; `TestLocalBrowserLoginRedeemsToKeychainWithoutBrowserPAT`; `TestLocalPATRotateFailurePreservesOldToken`; `TestLocalLogoutRevokesAndClearsKeychain`; `TestBrowserUIDoesNotStorePATOrCallCloudDirectly`; `TestLocalSessionDoesNotExposePAT`; `TestLoadConfigProductionRequiresDurableSecurity`; `go test ./...` from `cloud/`; `go test ./cmd/... ./internal/...` from `cli/`; UI `npm run build` | Browser-safe OAuth/PAT lifecycle now exists for configured OAuth providers and existing project members. First-user/project auto-provisioning, provider setup UX, and real OAuth provider/manual e2e evidence remain missing; normal production claim still blocked. | Add real provider/manual e2e evidence and first-user/project onboarding policy before marking Implemented |
| SEC-05 | Secret reveal gated by Owner + OTP/TOTP | Partial | Agent secret service; Cloud OTP; `cli/internal/commands/start.go`; `cli/ui/features/secrets/secrets-view.tsx` | Non-owner reveal, missing second-factor, verified-role, OTP expiry/one-time/rate-limit tests; `TestLocalSecretRevealRequiresExplicitIntent`; `TestLocalSecretRevealUsesAgentWithNoStorePolicy`; `TestLocalSecretAgentErrorIsRedacted`; `TestServiceAuditNeverIncludesSecretValue` | Local create/reveal/rotate exists through CLI backend and Agent; secret list/delete/secret-audit endpoint and full runtime audit merge remain missing | Add Agent-backed metadata listing/delete/audit merge or keep unsupported with typed errors |
| SEC-08 | Kubernetes secret values are not passed in process arguments | Implemented | `agent/internal/secret/store.go`; `agent/internal/secret/store_test.go` | `TestKubernetesSecretStorePutUsesStdinManifestNoArgSecrets`; `TestKubernetesSecretStorePutTOTPUsesStdinManifestNoArgSecrets`; `TestKubernetesSecretStoreApplyErrorRedactsSecretValues`; `TestKubernetesSecretStoreInvalidInputFailsBeforeKubectl`; ` env GOCACHE=/tmp/opsi-go-cache GOTOOLCHAIN=local go test ./internal/secret` | Broader local UI secret endpoints still tracked under SEC-05/A-01; this row only covers Agent kubectl argv handling | Keep grep/tests blocking any future `--from-literal` secret write path |
| CS-03 | Cloud does not store raw operational payloads | Partial | Local telemetry stores; AI payload sanitizer; `cloud/internal/webhookrelay/store.go`; `relay_jobs.redacted_body` | Sanitizer/redaction tests; webhook relay raw body omission/idempotency tests; mandatory `make verify-postgres` redaction checks for Postgres relay body/error/event persistence | Current webhook relay persists sanitized envelope metadata only; broader external notification/outbox payload policy remains future work | Keep raw runtime payload storage blocked for future relay/outbox additions |
| OBS-02 | AI RCA uses sanitized context | Partial | Incident context schema gate; fixture/Gemini adapter metadata; Agent RCA metadata hash validation; Agent incident response `rca_metadata`; CLI local incident list/detail/analyze/approve/resolve proxy | Raw-log/secret-like AI payload rejection, Incident sanitizer, RCA metadata hash, fallback metadata, unsafe action, stale-action, approval audit tests; `TestLocalIncidentListUsesAgentNotCloud`; `TestLocalIncidentAnalyzeUsesAgentNotCloud`; `TestLocalIncidentRejectsArbitraryCommand`; `TestLocalIncidentApproveSendsAllowlistActionHashToAgent` | Browser-to-local incident workflow is Agent-backed; Gemini output still maps mostly to root-cause text; actions remain fixture-typed | Expand provider schema without allowing free-form execution |
| SEC-06 | Audit trail for sensitive actions | Partial | Cloud/Agent audit paths; Postgres append-only trigger; `/api/local/projects/{project_id}/audit` proxy | Audit/RBAC/local proxy tests | Runtime audit merge with Agent audit is still pending | Merge Agent runtime audit into local audit endpoint |
| SEC-07 | Deployment source paths cannot escape checkout root | Implemented | `agent/internal/deploy/path_security.go`; Agent deployment request validation; Engine path resolution before build/apply | `agent/internal/deploy/path_security_test.go`; `agent/internal/deploy/types_test.go`; ` env GOCACHE=/tmp/opsi-go-cache GOTOOLCHAIN=local go test ./internal/deploy`; ` env GOCACHE=/tmp/opsi-go-cache GOTOOLCHAIN=local go test ./...` from `agent/` | Cloud registry still stores draft path strings as metadata; Agent enforces before runtime execution | Add Cloud-side draft validation when rewriting full SRS v4 traceability matrix |
| LF-TELEM-01 | Local UI telemetry summary comes from Agent/local runtime | Partial | `contracts/agent/v1/status.proto`; `contracts/go/agentv1/status.go`; `agent/internal/server/server.go` `TelemetryService.QueryTelemetry`; `agent/internal/telemetry/query.go`; `agent/internal/telemetry/kubernetes.go`; `cli/internal/commands/start.go`; `cli/ui/lib/api/local-client.ts`; `cli/ui/features/logs/logs-view.tsx` | `TestLocalTelemetrySummaryUsesAgentAndHidesRawPayload`; `TestLocalLogsUseAgentNotCloudAndRedact`; `TestLocalTelemetryInvalidInputFailsClosed`; `TestBuildQueryResponseRedactsLogsAndSummarizesRuntime`; `TestKubernetesCollectorMapsPodMetricsAndLogs`; ` env GOCACHE=/tmp/opsi-go-cache GOTOOLCHAIN=local go test ./internal/commands ./internal/agentclient`; ` env GOCACHE=/tmp/opsi-go-cache GOTOOLCHAIN=local go test ./internal/telemetry ./internal/server`; ` env GOCACHE=/tmp/opsi-go-cache GOTOOLCHAIN=local go test ./agentv1` | Agent-backed summary, service health, pod ready/restart counts, and cursor-paginated recent logs exist; broader incident correlation/SLO dashboards/export workflows remain outside this task | Keep raw log export unsupported until explicit user action plus audit exists |
| BOOTSTRAP-01 | Add Server bootstrap worker path is truthful and fail-closed | Partial | `cloud/cmd/opsi-bootstrap-worker`; `cloud/internal/bootstrapworker`; `cloud/internal/webhookrelay/bootstrap_agent_api.go`; `cloud/internal/registry/service.go`; `cloud/internal/registry/postgres.go`; `Makefile` | `TestConfigValidation`; `TestRunOnceSupportedCompletesAfterHeartbeatVerification`; `TestRunOnceFailureModesFinishFailed`; `TestRunOnceRedactsSecretsInProgressFinishAndErrors`; `TestUnsupportedAndMissingConfigFailClosed`; `TestValidateBundleInvalidTargetFailsClosed`; `TestBootstrapCredentialVaultAndRBAC`; ` go test ./internal/bootstrapworker ./internal/webhookrelay ./...` from `cloud/` | Worker now has a real SSH/password Ubuntu first-server path: take credential, connect over SSH, preflight Ubuntu/curl/systemd/sudo, install K3s server, download configured Opsi Agent binary, exchange one-time registration token on the target, write Agent cloud-relay config, start systemd Agent, wait for Cloud session state set by healthy Agent heartbeat, then finish completed. Cloud rejects SSH private-key upload. Private-key auth, worker-node join, non-Ubuntu targets, missing installer URL, and missing production known_hosts path fail closed. Clean VPS manual/e2e evidence, binary distribution/checksum release proof, and HA/join coverage are still missing. | Run real clean Ubuntu VPS Add Server e2e; add signed/checksummed Agent artifact release path and worker-node join before any production-ready claim |
| E2E-K3S-01 | Clean VPS/K3s end-to-end proof path | Partial | `Makefile` `verify-e2e-k3s-preflight`, `verify-e2e-k3s`, `verify-e2e-k3s-selfcheck`; `scripts/e2e/verify-k3s.sh`; `test/e2e/sample-service`; `test/e2e/sample-service-bad`; `.github/workflows/e2e-k3s.yml`; `docs/runbooks/clean_vps_k3s_e2e.md` | `bash -n scripts/e2e/verify-k3s.sh`; `./scripts/e2e/verify-k3s.sh --self-test`; `make verify-e2e-k3s-preflight` fails closed when required tools/env are missing | Canonical command and protected manual workflow exist. Full pass requires a clean Ubuntu VPS, running local backend/Cloud/bootstrap worker/Agent release path, SSH password bootstrap access, second factor, service Git SHA, and explicit mitigation approval. No real clean VPS/K3s pass artifact is recorded yet in this repo. | Run `make verify-e2e-k3s` against protected infra, review redacted `.tmp/e2e-k3s/<run-id>` evidence, then attach result to release notes/status |
| K3S-NODE-01 | Drain/remove execute through Agent/K3s or are blocked honestly | Partial | `agent/internal/nodelifecycle`; `agent/internal/cloudrunner`; `agent/internal/cloudrelay`; `cloud/internal/registry`; `cloud/internal/webhookrelay/registry_api.go`; `cloud/internal/webhookrelay/server.go`; `cloud/internal/postgres/migrate.go`; `scripts/e2e/verify-node-lifecycle.sh`; `Makefile` `verify-e2e-node-lifecycle-preflight`, `verify-e2e-node-lifecycle`, `verify-e2e-node-lifecycle-selfcheck`; `docs/runbooks/node_lifecycle_k3s_e2e.md` | `TestDrainExecutesTypedKubectlAndVerifies`; `TestInvalidTargetFailsBeforeKubectl`; `TestK3sFailureIsFailedAndRedacted`; `TestNodeLifecycleRequiresAgentResultForCompletion`; `TestNodeLifecycleVerifiedDrainUpdatesNodeAndIsIdempotent`; `TestBootstrapCredentialVaultAndRBAC`; `make verify-e2e-node-lifecycle-selfcheck`; ` env GOCACHE=/tmp/opsi-go-cache GOTOOLCHAIN=local go test ./internal/nodelifecycle ./internal/cloudrunner ./internal/cloudrelay ./internal/server` from `agent/`; ` env GOCACHE=/tmp/opsi-go-cache GOTOOLCHAIN=local go test ./internal/registry ./internal/webhookrelay` from `cloud/` | Agent-owned typed cordon/drain/remove exists through Cloud lifecycle job lease/result and verified Agent result. Cloud cannot mark terminal success without Agent verification. Canonical real K3s command exists and default-proves drain with kubectl verification; remove is manual/destructive gated by `OPSI_E2E_ALLOW_NODE_REMOVE=1`. No real worker-node pass artifact is recorded yet; HA server removal safety remains basic only-server guard plus explicit force/remove intent. | Run `make verify-e2e-node-lifecycle` against protected worker-node infra, then rerun with explicit remove opt-in and attach redacted `.tmp/e2e-node-lifecycle/<run-id>` evidence before production-ready claim |
| DX-03 | Production packaging and source hygiene | Partial | `README.md`; `Makefile` `verify`, `clean`, `package-source`; `.gitignore`; `docs/repair/00_FIX_SCOPE_AND_GATES.md` | `make verify` including Go vet/tests, UI `npm ci`, build/typecheck, and lint; `make test`; `make build`; `make clean`; `make source-hygiene`; `make package-source` | Installer/service units missing | Keep generated artifacts out of source; add installer/service units later |
| REL-DR-01 | Backup/restore and disaster recovery proof | Partial | `scripts/opsi-backup.sh`; `scripts/opsi-restore.sh`; `scripts/verify-dr.sh`; `agent/internal/dr`; `cloud/internal/dr`; `docs/runbooks/backup_restore_dr.md` | `make verify-dr`; `go test ./internal/dr` from `agent/`; `go test ./internal/dr` from `cloud/` | Tested restore covers Cloud Postgres control-plane state and Agent SQLite metadata; Cloud encryption key material, K3s datastore/manifests, managed service user data, migration rollback, and timed real-region RTO exercise remain outside coverage | Add external secret/KMS key recovery, K3s/managed-service data backup, release-note evidence, and real environment timed DR exercise before any production-ready claim |
