"use client";

import { Empty } from "@/components/ui/primitives";
import { DeliveryStatus, Evidence, displayTime, short } from "@/features/delivery/shared";
import type { ConsoleController } from "@/features/console/types";
import type { DeliveryData } from "@/features/delivery/data";
import { useSelectedDeployment } from "@/features/delivery/polling";
import { LocalClient } from "@/lib/api/local-client";
import type { DeploymentJob } from "@/lib/contracts/registry";

export function DeploymentDetail({ console, data, selected }: { console: ConsoleController; data: DeliveryData; selected?: DeploymentJob }) {
  const projectID = console.state.project?.id ?? "";
  const polled = useSelectedDeployment(projectID, selected?.id ?? "", selected, data.mergeDeployment);
  const job = polled.job ?? selected;
  if (!job) return <aside className="detailPanel"><Empty title="Select a deployment" text="Choose a DeploymentJob to inspect artifact, rollout, verification, failure, rollback, and event evidence." /></aside>;
  const currentDigest = job.current_digest || job.terminal_result?.current_digest;
  const canCancel = job.status === "queued" && ["", "prepared", "queued"].includes(job.rollout_state ?? "");
  const canRetry = job.status === "failed" && job.failure_code === "DEPLOYMENT_LEASE_ATTEMPTS_EXHAUSTED" && !job.terminal_result;
  const canCleanup = Boolean(job.snapshot?.preview) && !["cleaning", "cleaned"].includes(job.rollout_state ?? "");
  const client = new LocalClient();

  function mutate(operation: "cancel" | "retry" | "rollback" | "cleanup") {
    if (!job) return;
    const exact = operation === "rollback" || operation === "cleanup";
    console.reviewMutation({ project: console.state.project?.name || projectID, targetType: job.snapshot?.preview ? "preview deployment" : "deployment", targetID: job.id, operation, diff: [operation === "rollback" ? "restore the exact previous known-good snapshot" : operation === "cleanup" ? "remove active preview runtime resources" : operation === "retry" ? "retry the same unresolved DeploymentJob" : "cancel before the unsafe execution boundary"], risk: operation === "rollback" || operation === "cleanup" ? "Destructive runtime mutation; the exact job identity is required." : "The backend remains authoritative and may reject stale eligibility.", confirmation: exact ? job.id : undefined }, async (key) => {
      const result = operation === "cancel" ? await client.deploymentCancel(projectID, job.id, key) : operation === "retry" ? await client.deploymentRetry(projectID, job.id, key) : operation === "rollback" ? await client.rollback(projectID, job.id, key) : await client.previewCleanup(projectID, job.id, key, "manual");
      data.mergeDeployment(result);
      await data.refreshDeployments();
      return `${operation} returned factual state ${result.rollout_state || result.status} for ${result.id}.`;
    });
  }

  return <aside className="detailPanel" aria-label="Selected deployment detail"><div className="detailHeading"><div><p className="eyebrow">Deployment Detail</p><h2>{data.services.find((service) => service.id === job.service_id)?.name ?? job.service_id}</h2><p>{job.environment_id || "Environment not reported"} · {short(job.desired_digest || job.snapshot?.image.digest, 22)}</p></div><DeliveryStatus status={job.rollout_state || job.status} /></div>{polled.error ? <p aria-live="polite" className="truthCallout">{polled.error}</p> : null}<section><h3>Identity</h3><dl className="evidenceGrid"><Evidence label="Deployment ID" value={job.id} mono /><Evidence label="BuildRecord ID" value={job.snapshot?.authority.build_record.id} mono /><Evidence label="Created" value={displayTime(job.created_at)} /><Evidence label="Finished" value={displayTime(job.finished_at)} /></dl></section><section><h3>Artifact & Target</h3><dl className="evidenceGrid"><Evidence label="Immutable reference" value={job.snapshot?.image.reference} mono /><Evidence label="Desired digest" value={job.desired_digest} mono /><Evidence label="Current digest" value={currentDigest} mono /><Evidence label="Runtime / node" value={`${job.runtime_id || "Not reported"} / ${job.node_id || "Not reported"}`} /></dl></section><section><h3>Rollout & Verification</h3><dl className="evidenceGrid"><Evidence label="Rollout state" value={job.rollout_state || job.status} /><Evidence label="Attempt" value={`${job.attempt_count ?? 0} / ${job.max_attempts ?? "Not reported"}`} /><Evidence label="Application image ID" value={job.terminal_result?.application_image_id} mono /><Evidence label="Available replicas" value={job.terminal_result ? job.terminal_result.available_replicas : undefined} /><Evidence label="Readiness evidence" value={job.readiness_evidence_hash || job.terminal_result?.readiness_evidence_hash} mono /></dl></section>{job.failure_code || job.rollback_eligible || job.rollout_state === "rolled_back" ? <section><h3>Failure & Rollback</h3><dl className="evidenceGrid"><Evidence label="Failure" value={job.failure_code ? `${job.failure_code}: ${job.failure_message_redacted || "No redacted message"}` : "No failure reported"} /><Evidence label="Known-good" value={job.known_good_id || job.terminal_result?.known_good_id} mono /><Evidence label="Previous digest" value={job.previous_digest} mono /><Evidence label="Rollback eligibility" value={job.rollback_eligible ? "Eligible" : job.rollback_blocked_reason || "Not eligible"} /></dl></section> : null}<div className="detailActions">{canCancel ? <button onClick={() => mutate("cancel")} type="button">Cancel Before Mutation</button> : null}{canRetry ? <button onClick={() => mutate("retry")} type="button">Retry Same Job</button> : null}{job.rollback_eligible ? <button onClick={() => mutate("rollback")} type="button">Review Exact Rollback</button> : null}{canCleanup ? <button onClick={() => mutate("cleanup")} type="button">Clean Up Preview</button> : null}</div><section><h3>Events</h3>{polled.events.length ? <ol className="eventTimeline">{polled.events.map((event) => <li key={event.id}><span aria-hidden="true" /><div><strong>{event.step}</strong><p>{event.message_redacted}</p><small>{displayTime(event.created_at)} · {event.progress_percent}% · attempt {event.attempt ?? 0}</small><details><summary>Technical Evidence</summary><code>request {event.request_id || "not reported"}</code></details></div></li>)}</ol> : <Empty title="No events reported" text="The Local API has not returned DeploymentJob events for this selection." />}</section><details><summary>Technical Evidence</summary><dl className="evidenceGrid"><Evidence label="Intent hash" value={job.intent_hash} mono /><Evidence label="State hash" value={job.rollout_state_hash} mono /><Evidence label="Spec hash" value={job.spec_hash} mono /><Evidence label="Known-good hash" value={job.known_good_hash} mono /></dl></details></aside>;
}
